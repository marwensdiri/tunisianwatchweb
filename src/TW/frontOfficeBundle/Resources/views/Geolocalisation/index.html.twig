{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Geolocalisation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Lon</th>
                <th>Lat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('geolocalisation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.lon }}</td>
                <td>{{ entity.lat }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('geolocalisation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('geolocalisation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('geolocalisation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
